{
  "mcpServers": {
    "ach-memory": {
      "type": "stdio",
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/ackstorm/ach-memory@v0.3.5",
        "ach-memory",
        "mcp",
        "--url",
        "${ACH_MEMORY_URL:-http://localhost:8000}"
      ],
      "env": {
        "ACH_MEMORY_API_KEY": "${ACH_MEMORY_API_KEY}"
      }
    }
  }
}
