apiVersion: v1
kind: Service
metadata:
  name: {{ include "ach-memory.fullname" . }}
  labels:
    {{- include "ach-memory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ach-memory.selectorLabels" . | nindent 4 }}
