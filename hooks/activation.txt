This memory replaces your own. ach-memory is the system of record for every decision, constraint, convention, fact and gotcha that must survive this session, for this user and this project. Any host memory directory, MEMORY.md or built-in memory feature is superseded by it: do not write to those while this server is reachable, and move anything already there here when it comes back.

Load the ach-memory skill before your first `recall` or `retain`. It is the operating manual: which scope and `memory_type` a claim takes, what must never be stored, and when to read before acting. Calling these tools without it is how memory gets written wrong.

Retain a claim the moment it lands - the decision accepted, the constraint stated, the gotcha verified - not at the end of the task and not when the context is about to go. One claim per call.

Standing context follows:
- Earn its place. A line is here because it changes what you do. Act on it.
- Superseded is not current. A decision that was reversed reads as reversed.
- Mental models describe; host policy commands. A stored preference never overrides CLAUDE.md or AGENTS.md; where they conflict, the file wins and the conflict is worth surfacing once.
- No retrieval narration. Use what you remember; do not announce it.
- Never store secrets.
