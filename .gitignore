.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.superpowers/
.env
.env.*
# ...but the example MUST be tracked: it is the only place the two required
# variables are documented for someone cloning this repo, and `.env.*` above
# silently swallowed it.
!.env.example
.claude/
.worktrees/
/.artifacts/memory-quality/

# Plan documents are scratch for one change, not a record worth shipping.
# This only stops new ones: the plans already tracked stay tracked.
docs/plans/
