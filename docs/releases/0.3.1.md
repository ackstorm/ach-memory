# 0.3.1

- `init claude` / `init codex`: the host CLIs exit 0 on "already added / already
  installed" without refreshing, so a re-run never updated. Marketplace update
  and plugin update (Codex: marketplace upgrade + re-add) now run unconditionally.

No service change; no production roll.
