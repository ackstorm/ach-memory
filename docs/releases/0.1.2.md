# 0.1.2

- Project bank ids are random uuids (`project_<uuid4>`), no longer derived from
  the slug. A rename stays in `project_slugs`; the engine never sees it. No
  migration: production had no project rows.
