# 0.1.3

- Built-in mental models (`user-context`, `project-context`) now carry
  `trigger: {mode: delta, refresh_after_consolidation: true}`. Without it
  Hindsight generated them once at bank creation and never again; production
  models created by 0.1.0-0.1.2 are patched at the bank's next retain.
